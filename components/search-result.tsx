import { useSearchParams } from 'next/navigation'
export default function SearchResult() {
  return (
    <div>
      {/* {movies.results?.map((mv: Movie) => { */}
      {/*   return <Card key={mv.id} {...mv} prefix={mv.media_type} /> */}
      {/* })} */}
    </div>
  )
}
