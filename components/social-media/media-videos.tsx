'use client'
import { fetchMovies } from '@/lib/data'
import { VIDEOS_KEY } from '@/lib/endpoint'
import { useEffect } from 'react'
export default function MediaVideos({ movieId }: { movieId: string }) {
  return <div>this is MediaVideos</div>
}
